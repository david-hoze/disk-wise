# CLAUDE.md

## Build

- Build with `cabal build`, not stack
- Run tests with `cabal test`

## Rules

- Never use the tool (diskwise) creatively to work around problems in the tool itself. If the tool doesn't work correctly, fix the source code instead of trying clever invocations or manual workarounds.
